# Daemon — requirements.txt
# Python 3.11+ required
# Windows only

# --- Core GUI ---
PyQt6>=6.7.0

# --- Global input monitoring (APM tracker) ---
# Note: may require admin rights or accessibility permissions on Windows
# If import fails at runtime, APM monitor disables gracefully
pynput>=1.7.7

# --- Environment / Secrets ---
python-dotenv>=1.0.0

# --- HTTP / MCP (Model Context Protocol) ---
# Pinned below 2.0: mcp 2.x removed mcp.server.fastmcp (the API used by
# src/mcp_server.py). Upgrading requires migrating to the new MCPServer API.
mcp>=1.0.0,<2.0.0
uvicorn>=0.30.0          # FastMCP SSE server + direct import in mcp_server.py
requests>=2.31.0
aiohttp>=3.9.0           # observability.py metric/health web server

# --- Windows UI Automation / win32 ---
comtypes>=1.4.8
pywin32>=306             # pythoncom / pywintypes (screen reader, COM init)
pywinauto>=0.4.11        # mcp_server.py execute_os_action window control

# --- Firebase Admin SDK (service account for Firestore) ---
firebase-admin>=6.0.0
Pillow>=10.0.0           # ImageGrab.grab() for capture_blackmail_evidence

# --- Text-to-Speech ---
edge-tts>=6.1.0
pyttsx3>=2.90            # fallback TTS engine
pydub>=0.25.1            # pitch-shift edge-tts mp3
# simpleaudio NOT required: only an optional last-resort fallback in
# src/system/tts_worker.py when winsound playback fails, and it has no
# prebuilt wheel for Python 3.14 (needs MSVC Build Tools to compile).

# --- Observability / Production Logging ---
structlog>=24.1.0
prometheus-client>=0.19.0
opentelemetry-api>=1.24.0
opentelemetry-sdk>=1.24.0
opentelemetry-exporter-otlp-proto-http>=1.24.0
opentelemetry-instrumentation-requests>=0.45b0

# --- External CLI dependency (not pip-installable) ---
# opencode CLI must be installed separately and on PATH

# --- Build ---
pyinstaller>=6.0.0

# --- Testing ---
pytest>=8.0.0