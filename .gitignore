# Python
__pycache__/
*.pyc

# pytest
.pytest_cache/

# Pip artifacts (=1.0.0 etc.)
=*

# Runtime / scratch files
*.tmp
*_response.json

# Secrets
.env

# Credentials
assets/firebase-credentials.json
data/firebase-credentials.json

# Log files
logs/

# Local storage (memory, history, diary, config, lock, cache, thoughts)
data/
!data/brain_schema.json

# Temp test artifacts
crash_dump.log
this_file_surely_does_not_exist_98765.json

# Strands artifact (occasionally generated at project root)
/strands_result.py

# Root-level runtime state files (should live under data/ but guard against cwd writes)
/.daemon_*

# Hermes Agent tooling artifacts (plan files, session state)
.hermes/
.worktrees/